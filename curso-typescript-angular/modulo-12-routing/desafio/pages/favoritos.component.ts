/*
 * DESAFIO — favoritos.component.ts
 * Placeholder "em construção" pra rota /favoritos.
 *
 * Esse componente é carregado em lazy (loadComponent no app.routes.ts).
 * Abra a aba Network do DevTools e veja: o JS dele só baixa quando
 * você clica no link "Favoritos" pela primeira vez.
 */

import { Component } from '@angular/core';

@Component({
  selector: 'app-favoritos',
  standalone: true,
  template: `
    <h2>Favoritos</h2>
    <p>🚧 Em construção... volte em breve!</p>
  `,
})
export class FavoritosComponent {}
